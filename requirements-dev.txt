# зависимости тестовой стадии Docker-сборки (FROM builder AS test, Dockerfile).
# ставятся поверх requirements.txt в тот же venv и в боевой слой не попадают:
# боевой слой копирует /opt/venv из стадии builder, то есть до этого файла.
# смысл разделения — не тащить тестовый фреймворк в образ, который держит
# переписку и пароль от Exchange
pytest==9.1.1
