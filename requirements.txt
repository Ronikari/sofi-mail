# Клиент Open WebUI: он отдаёт OpenAI-совместимый API,
# отдельного клиента у него нет
langchain-openai==1.4.1
typer==0.26.8
python-dotenv==1.0.1

# транспорт почты: Exchange Web Services — единственный способ ходить
# за письмами, поэтому зависимость обязательная
exchangelib==5.6.0

# Библиотек разбора документов здесь нет. Файл из письма уходит в Open WebUI
# как есть, разбор и индексацию выполняет сервер: у него свой набор парсеров
# и распознавание текста, обновляемые без пересборки этого образа.
# Прежде здесь стояли unstructured[docx,pptx,xlsx,md] и pdfminer.six — около
# гигабайта в образ и второй, расходящийся с сервером, набор парсеров.

# разбор письма — stdlib (email), разбор HTML — html.parser
