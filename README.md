# sofi-mail

Чат с локальной LLM через электронную почту вместо веб-интерфейса.

За моделью закреплён отдельный почтовый ящик. Пользователь пишет туда письмо: тело письма
становится промптом, тема — названием сессии. Демон забирает письмо, генерирует ответ локальной
моделью и отправляет его на адрес отправителя. Если пользователь отвечает (Reply)
на письмо модели, вся переписка треда сохраняется и учитывается при генерации. Новое письмо
с новой темой начинает новую сессию.

```
EWS (polling) ──> разбор письма ──> сессия ──> модель ──> ответ письмом
Exchange          отсечение цитат   SQLite     vLLM       Re: + In-Reply-To
                                               N потоков  (готовый MIME в EWS)
```

## Установка

Нужен **Python 3.9 или новее** (в коде используется `str.removesuffix`).

```bash
uv venv && uv pip install -r requirements.txt
cp .env.example .env
```

Заполнить в `.env` пять обязательных полей: `MAIL_ADDRESS`, `MAIL_PASSWORD`, `EWS_SERVER`,
`LLM_BASE_URL`, `ALLOWED_SENDERS`. Остальное — рабочие дефолты.

### Почтовый ящик Exchange

Нужен **отдельный** ящик под модель, **в который не заходит человек**: демон забирает
непрочитанные письма, и открытый в Outlook ящик «съедал» бы их областью чтения. Альтернатива —
серверное правило, складывающее письма в отдельную папку, тогда её имя идёт в `EWS_FOLDER`.

Работаем через Exchange Web Services — тот же протокол, которым ходит Outlook, и единственный,
который в Exchange включён всегда:

```dotenv
MAIL_ADDRESS=llm@company.ru          # адрес ящика, от него уходят письма
MAIL_LOGIN=CORP\svc-llm              # логин служебной учётки, если он не равен адресу
MAIL_PASSWORD=<пароль служебной учётки>
EWS_SERVER=mail.company.ru           # пусто = autodiscover, но в закрытых сетях он часто недоступен
EWS_AUTH=ntlm                        # gssapi — по билету Kerberos, пароль тогда не нужен
EWS_ACCESS_TYPE=delegate             # impersonation, если учётка работает «от имени» ящика
```

**Basic и NTLM отключены, оставлена современная аутентификация** (`EWS_AUTH=oauth2`) — нужна
регистрация приложения в Entra ID с правом `full_access_as_app` на EWS. Вариантов два, и выбор
между ними — по наличию `MAIL_PASSWORD`:

```dotenv
EWS_AUTH=oauth2
EWS_CLIENT_ID=00000000-0000-0000-0000-000000000000
EWS_CLIENT_SECRET=...
EWS_TENANT_ID=...                    # пусто = тенант выберет Microsoft
MAIL_PASSWORD=                       # пусто: приложение работает от своего имени
EWS_ACCESS_TYPE=impersonation        # и тогда это обязательно
```

Пустой `MAIL_PASSWORD` — режим службы: пароль пользователя нигде не хранится, токен выдаётся
приложению, а ящик указывается заголовком impersonation (учётке нужно право
`ApplicationImpersonation`). Если пароль задан, приложение действует от имени пользователя, и
`EWS_ACCESS_TYPE=delegate` работает как обычно — но такой поток (ROPC) в тенантах часто запрещён
политикой. Несовместимую пару `oauth2` + `delegate` без пароля `check` отвергает на старте: Exchange
отвечает на неё `ErrorAccessDenied` при первом же чтении папки, и по этому ответу причину не угадать.

*Необходимо уточнить:* какой режим аутентификации включён на EWS (от этого зависит
`EWS_AUTH`); адрес точки входа и корневой сертификат внутреннего УЦ; выдадут ли служебной учётке
право **Send As** на ящик — без него Exchange отклонит письмо с чужим `From`; каков потолок
`EwsMaxConcurrency` в политике троттлинга — он ограничивает `WORKERS`.

### Модель

```dotenv
LLM_BASE_URL=https://llm.company.ru:8000/v1  # суффикс /v1 допишется, если забыть
LLM_CA_FILE=/opt/llm-email-chat/certs/corp-root.pem
LLM_MODEL=Qwen/Qwen2.5-72B-Instruct
LLM_NUM_CTX=32768                            # обязано совпадать с --max-model-len сервера
LLM_MAX_TOKENS=2048
```

```bash
curl --cacert certs/corp-root.pem https://llm.company.ru:8000/v1/models   # список моделей
```

Адрес обязан быть `https`: в запросе уходит полный текст письма и вся история сессии, и по
`http` это открытый текст в корпоративной сети — демон с `http`-адресом не стартует. Если TLS
на сервере модели поднять нельзя, а сегмент изолирован, отказ оформляется явно:
`LLM_ALLOW_INSECURE=true`.

`LLM_MODEL` сверяется со списком с сервера строго, точным равенством: это то имя, под которым
модель отдаётся сервером — путь репозитория либо значение ключа `--served-model-name`, если
администратор задал своё. `check` при несовпадении печатает доступные модели.

Дефолта у `LLM_BASE_URL` намеренно нет: молчаливое обращение к себе же выглядело бы как
«сервер недоступен», и причину искали бы на чужой машине вместо своего `.env`.

**`LLM_NUM_CTX` обязано совпадать с `--max-model-len` на сервере.** У vLLM окно задаётся
при старте сервера и в запросе не участвует, а здесь это лишь клиентское знание о нём — по нему
демон решает, сколько истории поместится. Занизили — переписка начнёт терять старые реплики
раньше, чем нужно; завысили — сервер отвергнет длинный запрос.

## Запуск

```bash
python -m src.cli check                    # диагностика: конфиг, база, сервер модели, ящик Exchange
python -m src.cli serve --dry-run -v       # первый запуск: ответы печатаются, письма не уходят
python -m src.cli serve                    # рабочий режим
python -m src.cli serve -w 4               # сервис на компанию: 4 письма одновременно
```

### Параллельная обработка

По умолчанию `WORKERS=1` — письма обрабатываются по одному, чего достаточно для личного ящика.
`WORKERS=4` (или флаг `-w 4`) обрабатывает письма пулом потоков. Три вещи, которые важно понимать:

- **Потолок задаёт не ваша ВМ, а два чужих лимита.** Со стороны модели это `--max-num-seqs`
  у vLLM: сервер держит собственную очередь, и если она уже `WORKERS`, письма выстроятся в неё.
  Со стороны Exchange — `EwsMaxConcurrency` в политике троттлинга. Клиентские потоки сверх
  меньшего из двух ничего не ускорят. `check` печатает напоминание, когда `WORKERS > 1`.
  У vLLM запас здесь обычно велик за счёт непрерывной батчёвки, и упереться скорее получится
  в Exchange, чем в модель.
- **Реплики одной сессии всё равно обрабатываются по очереди.** Иначе два письма одного треда
  прочитали бы одну и ту же историю, и каждый ответ был бы построен без учёта второго вопроса.
  Параллельно идут разные сессии, а не разные письма одного разговора.
- **Отметка «обработано» ставится в главном потоке**, после того как пул отработал: сессия EWS
  не рассчитана на команды из нескольких потоков одновременно. Пул HTTP-соединений `exchangelib`
  при этом поднимается под `WORKERS` (`max_connections=WORKERS + 1`), иначе письма ждали бы
  друг друга уже на транспорте.

## Команды

| Команда | Назначение |
|---|---|
| `serve` | демон: опрашивает ящик каждые `POLL_INTERVAL_SEC` и отвечает на письма; `-w N` — потоков |
| `once` | один проход по непрочитанным — основной инструмент отладки |
| `check` | проверка конфига, базы, сервера модели и доступа к ящику Exchange |
| `ask "текст" [-s ID]` | спросить модель из терминала, без почты |
| `sessions` | список сессий: id, число реплик, собеседник, тема |
| `history ID [--raw]` | переписка сессии; `--raw` — тело до отсечения цитаты (нужен `STORE_RAW_BODY=true`) |
| `send-test` | отправить тестовое письмо самому себе |
| `retry` | вернуть письма со статусом `error` в очередь |
| `purge [--days N]` | удалить переписку старше срока хранения (демон делает это и сам, раз в сутки) |
| `forget --session N \| --address A` | удалить переписку по требованию: сессию или все сессии адреса |
| `ingest-eml FILE [--show-parsed]` | прогнать сохранённое письмо через пайплайн без почты |

Флаг `-v` включает DEBUG-лог и принимается в любой позиции: и `cli -v serve`, и `cli serve -v`.

## Как устроено

| Модуль | Ответственность |
|---|---|
| `src/config.py` | единственная точка чтения `.env` |
| `src/storage.py` | SQLite: сессии, реплики, журнал обработанных писем |
| `src/email_parser.py` | разбор MIME, кодировки, отсечение цитат, фильтры петель |
| `src/transport.py` | контракт почты: пять операций, которыми пользуется пайплайн |
| `src/ews_client.py` | приём и отправка через Exchange Web Services |
| `src/reply_builder.py` | сборка ответа с заголовками треда (MIME, который EWS отдаёт как есть) |
| `src/llm.py` | системный промпт, сборка контекста, бюджет окна, отсечение рассуждений |
| `src/llm_backend.py` | обращение к серверу vLLM: адрес, TLS, сверка имени модели |
| `src/redact.py` | маскирование адресов и тем писем в логах |
| `src/pipeline.py` | оркестрация: письмо → сессия → ответ → письмо, пул воркеров |
| `src/cli.py` | Typer, единая точка входа |

Модули самостоятельно не запускаются: `once` и `serve` вызывают один и тот же `process_email`,
поэтому отлаженное вручную поведение совпадает с поведением демона.